#include <stack>
#include <iostream>

using namespace std;

struct node
{
  int data;
  map<int, stuct node*> links;
}

struct node
{
  int key;
  int value;
}

int main()
{
  stack<int> st;
  queue<node> q;
  list<node> l;

  {
    if( it->value == 10 )
      cout<<
  }
  vector< vector<int> > mat;
  vector<int> row;
  row.push_back(1);

  mat.push_back(row);
  v.push_back();
  int size = v.size();

  s.top();
  s.push(4);
  s.pop();
  q.push(4);
  q.pop();
  q.front();
}
